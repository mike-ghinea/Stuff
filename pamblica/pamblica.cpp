#include<fstream>
using namespace std;
ifstream cin("panglica.in");
ofstream cout("panglica.out");
int N,C,i,c[10001]
int main()
{
    cin>>N>>C;
    for(i=1;i<=N;i++)
    cin>>c[i];
    for(i=1;i<=N;i++)
    {
        if(v[i]!=v[])
    }
    return 0;
}
