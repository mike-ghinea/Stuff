#include<bits/stdc++.h>
#define DIM 701
using namespace std;
ifstream input("rama.in");
ofstream output("rama.out");
string line;
char m[DIM][DIM];
int main()
{
    input>>N>>M;
    for(i=1;i<=N;i++)
    {
        cin>>(m[i]+1);
        for(j=M;j>=1;j--)
        {
            if(m[i][j]==1)

        }
    }
}
